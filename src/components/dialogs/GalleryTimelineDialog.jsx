import { Dialog } from '@fluentui/react';
import TimelineScreens from 'components/panels/game/galleryTimelineScreens';
export const GalleryTimelineDialog = ({ open, setOpen }) => {
  return <TimelineScreens />;
};
